# Layer-specific variables for 00_prerequisites in dev environment
# Global variables (aws_region, environment, project_name, account_id) are provided via environments/dev.tfvars

layer = "00_prerequisites"
