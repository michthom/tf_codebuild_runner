# Layer-specific variables for 00_prerequisites in stg environment
# Global variables (aws_region, environment, project_name, account_id) are provided via environments/stg.tfvars

layer = "00_prerequisites"
