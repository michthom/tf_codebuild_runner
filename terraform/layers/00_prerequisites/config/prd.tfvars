# Layer-specific variables for 00_prerequisites in prd environment
# Global variables (aws_region, environment, project_name, account_id) are provided via environments/prd.tfvars

layer = "00_prerequisites"
